# `tenant` explícito por la misma razón que en membresias.yml (SDD §16.7).
inicial_one:
  membresia: activa_one
  tenant: advance_fitness
  monto: 80000
  fecha_pago: <%= Date.current - 10 %>
  periodo_inicio: <%= Date.current - 10 %>
  periodo_fin: <%= Date.current + 20 %>
  metodo: efectivo
  registrado_por: admin
