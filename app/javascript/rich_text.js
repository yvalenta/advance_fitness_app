// Editor de texto enriquecido (Fase 16.5): importado SOLO por las vistas
// que lo usan (admin de posts/novedades) vía content_for :head — Trix pesa
// ~200KB sin minificar y no tiene lugar en el bundle global.
import "trix"
import "@rails/actiontext"
