<% content_for :title, "Nuevo tenant — Portal comercial" %>

<%= render "shared/encabezado",
      titulo: "Nuevo tenant",
      descripcion: "Alta de un gimnasio, entrenador o influencer con su subdominio y admin inicial." %>

<div class="mx-auto -mt-8 w-full max-w-3xl px-4 pb-12">
  <div class="card animate-fade-up border border-base-300 bg-base-100 shadow-md">
    <div class="card-body">
      <%= render "form", tenant: @tenant, con_admin: true %>
    </div>
  </div>
</div>
